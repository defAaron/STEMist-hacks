services:
  - type: web
    name: honeydesk-api
    runtime: docker
    plan: starter
    dockerContext: ./backend
    dockerfilePath: ./backend/Dockerfile
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: APP_ENV
        value: production
      - key: DATABASE_URL
        value: sqlite:////var/data/honeydesk.db
      - key: CORS_ORIGINS
        sync: false
      - key: SIMULATE_TOKEN
        generateValue: true
      - key: REQUIRE_SIMULATE_TOKEN
        value: "true"
      - key: EXPOSE_API_DOCS
        value: "false"
      - key: TRUST_PROXY
        value: "true"
      - key: BRIEF_FAILOVER_CACHE
        value: "true"
      - key: FORCE_HTTPS
        value: "true"
    disk:
      name: honeydesk-data
      mountPath: /var/data
      sizeGB: 1
