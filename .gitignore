# Cursor IDE (local MCP/editor config)
.cursor/

# Local-only planning (agent + owner; not for GitHub)
docs/BUILDPLAN.md

# Environment files (secrets) — never commit
.env
.env.*
!.env.example
!.env*.example

# Node
node_modules/

# Next.js
frontend/.next/
frontend/out/
frontend/.env.local
